# StayHub Frontend

다음 단계에서 Vite + React 프로젝트를 이 폴더에 구성합니다.

현재 폴더 역할:

- `pages`: URL에 대응하는 화면
- `components`: 여러 화면에서 재사용하는 UI
- `contexts`: 로그인 사용자처럼 앱 전체에서 공유할 상태
- `services`: `fetch`를 이용한 백엔드 API 호출
- `hooks`: 반복되는 상태 로직
