# 시험 구현 순서

1. `backend` Express 프로젝트 생성 및 MongoDB 연결
2. `User` 모델 → 회원가입 → 로그인 → JWT 인증
3. `Hotel`, `Room` 모델과 목록/상세 조회 API
4. Postman에서 호텔 상세에 객실이 정상 노출되는지 확인
5. `frontend` React 프로젝트 생성 및 Router 적용
6. 호텔 목록/상세 화면을 API와 연결
7. 예약 중복 방지 API 구현 후 예약 화면 연결
8. 마이페이지·후기·삭제 권한·예외처리 마무리